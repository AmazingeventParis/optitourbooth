# ===========================================
# OptiTour Booth Backend - Variables d'environnement
# ===========================================
# Copiez ce fichier en .env : cp .env.example .env
# Puis modifiez les valeurs selon votre environnement

# ----- Base de données PostgreSQL -----
# Option 1 : PostgreSQL local (via Docker)
# DATABASE_URL=postgresql://optitour_user:optitour_secret_2024@localhost:5432/optitour?schema=public
# Option 2 : Neon (cloud) - demandez l'URL à l'admin du projet
DATABASE_URL=postgresql://user:password@host:5432/database?schema=public

# ----- Backend -----
NODE_ENV=development
PORT=3000
API_URL=http://localhost:3000

# ----- JWT Authentication -----
# Changez ces valeurs en production !
JWT_SECRET=changez_moi_secret_jwt_256_bits
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=changez_moi_refresh_secret_different
JWT_REFRESH_EXPIRES_IN=30d

# ----- OSRM (Routing) -----
OSRM_URL=https://router.project-osrm.org

# ----- Geocoding (Nominatim) -----
NOMINATIM_URL=https://nominatim.openstreetmap.org

# ----- VROOM - Optimisation de tournées -----
# Option 1: VROOM local via Docker
VROOM_URL=
VROOM_ENABLED=false
# Option 2: OpenRouteService API (gratuit, 1000 req/jour)
# Créez un compte sur https://openrouteservice.org/dev/#/signup
ORS_API_KEY=

# ----- TomTom - Trafic prédictif (optionnel) -----
# Créez un compte gratuit sur https://developer.tomtom.com
# 2500 requêtes/jour gratuites
TOMTOM_API_KEY=

# ----- Upload fichiers -----
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# ----- CORS -----
CORS_ORIGIN=http://localhost:5173
